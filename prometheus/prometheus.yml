global:
  scrape_interval: 10s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "ml_pipeline"
    static_configs:
      - targets: ["pipeline:8000"]
    metrics_path: /metrics
    scrape_interval: 5s
